apiVersion: apps/v1
kind: Deployment
metadata:
  name: mix-design-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: mix-design
          resources:
            requests:
              memory: "128Mi"
              cpu: "500m"
            limits:
              memory: "256Mi"
              cpu: "1"
